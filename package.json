{
  "name": "free-code-web",
  "version": "1.0.0",
  "description": "Web interface for free-code CLI",
  "type": "module",
  "scripts": {
    "dev": "node src/server/index.js",
    "build": "npm run build:client && npm run build:server",
    "build:client": "echo 'Static files served directly'",
    "build:server": "echo 'Server is plain JS'",
    "start": "NODE_ENV=production node src/server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "body-parser": "^1.20.2",
    "uuid": "^9.0.0"
  }
}

